---
title: "Heaps और Priority Queues - Interviews का Swiss Army Knife"
description: "Heaps, priority queue abstraction, और classic patterns जैसे top-K problems और sorted lists merge करना - सब सीखो।"
order: 6
difficulty: "intermediate"
duration: 18
icon: "⛰️"
published: true
---

## Heap क्या है?

**Heap** एक complete binary tree है जहाँ हर parent **heap property** satisfy करता है - या तो हमेशा छोटा (min-heap) या हमेशा बड़ा (max-heap) अपने children से। "Complete" का मतलब हर level भरा है, सिवाय शायद last level के जो left से right भरता है।

```
       1            Min-heap
      / \
     3   5
    / \
   7   4
```

Complete होने की वजह से इसे **flat array** में store करते हैं - pointers की ज़रूरत नहीं। Index `i` के लिए: left child = `2i + 1`, right child = `2i + 2`, parent = `(i - 1) // 2`।

## Min-Heap vs Max-Heap

| Property | Min-Heap | Max-Heap |
|----------|----------|----------|
| Root | सबसे छोटा element | सबसे बड़ा element |
| Parent ≤ Children? | ✅ हाँ | ❌ नहीं (≥) |
| Extract देता है | Minimum | Maximum |

Python का `heapq` default में **min-heap** है। Max-heap के लिए insert पर values negate करो और extract पर वापस negate करो।

## Insert और Extract - Step by Step

**Insert** - end पर push करो, फिर **bubble up** (parent से swap करो जब तक छोटा हो):

```python
import heapq
h = [1, 3, 5, 7]
heapq.heappush(h, 2)  # h becomes [1, 2, 5, 7, 3]
```

**Extract min** - root को last element से swap करो, last pop करो, फिर **sift down** (smaller child से swap):

```python
smallest = heapq.heappop(h)  # returns 1
```

दोनों operations **O(log n)** में चलते हैं क्योंकि tree की height log n है।

<Illustration src="/images/lessons/heaps-and-priority-queues.svg" alt="Min-heap पर insert bubble-up और extract sift-down दिखाने वाला diagram" caption="Insert bubble up करता है; extract-min sift down करता है।" />

## Heapify - O(n) में Heap बनाओ

`heapq.heapify(arr)` एक list को **in-place** O(n) में heap बना देता है, O(n log n) नहीं। यह bottom-up काम करता है, हर non-leaf node को sift down करता है।

<FunFact>Heap बनाना O(n) है, O(n log n) नहीं। ज़्यादातर nodes bottom के पास हैं और बमुश्किल sift down करने पड़ते हैं - maths linear time पर settle करता है।</FunFact>

## Priority Queue Abstraction

**Priority queue** हमेशा highest-priority item पहले access करने देता है। Heaps इसका best implementation हैं क्योंकि insert और extract दोनों O(log n) हैं। जब भी "अब तक का best item दो" चाहिए, priority queue इस्तेमाल करो।

## Pattern: Top-K Problems

> "Unsorted array में K largest elements ढूँढो।"

**Size K का min-heap** रखो। हर element push करो; अगर heap K से बड़ा हो जाए, smallest pop करो। जो बचे वो K largest हैं।

```python
import heapq
def top_k(nums, k):
    return heapq.nlargest(k, nums)
```

Time: **O(n log k)** - जब k ≪ n हो तो sorting से काफ़ी बेहतर।

<Quiz question="10 लाख entries में से 5 सबसे बड़े scores चाहिए। कौन सा heap type और size?" options="Max-heap of size 10,00,000|Min-heap of size 5|Max-heap of size 5|Min-heap of size 10,00,000" answer="1" explanation="Size 5 का min-heap अब तक के 5 सबसे बड़े रखता है। Root (5 में सबसे छोटा) gatekeeper है - कोई भी छोटा element reject हो जाता है।" />

## Pattern: Merge K Sorted Lists

हर list का head min-heap में push करो। Smallest pop करो, फिर उसी list का next element push करो। जब तक सब lists खत्म न हो जाएँ। Time: **O(N log K)** जहाँ N total elements हैं।

## Pattern: Median from Data Stream

दो heaps maintain करो: lower half के लिए **max-heap** और upper half के लिए **min-heap**। Sizes में maximum 1 का difference रखो। Median हमेशा एक या दोनों roots पर होता है।

```
Lower half (max-heap): [1, 2, 3]  ← max = 3
Upper half (min-heap): [4, 5, 6]  ← min = 4
Median = (3 + 4) / 2 = 3.5
```

<Quiz question="Two-heap median approach में नया element पहले कहाँ जाता है?" options="हमेशा min-heap|हमेशा max-heap|जो heap छोटा हो|Max-heap root से compare करो, फिर rebalance" answer="3" explanation="पहले max-heap (lower half) में push करो, फिर ज़रूरत हो तो root min-heap में move करो, और sizes rebalance करो।" />

## Scheduling Problems

Heaps scheduling में कमाल करते हैं: meeting rooms (earliest end-time track करो), task scheduling with cooldowns, या CPU job queues। मूल बात: heap efficiently track करता है "अगला क्या खत्म होगा।"

<ThinkAboutIt>अगर minimum और maximum दोनों efficiently चाहिए, तो एक heap काफ़ी नहीं। कौन सा data structure combination इस्तेमाल करोगे?</ThinkAboutIt>

## Heap vs Sorted Array vs BST

| ज़रूरत | Best choice | क्यों |
|--------|-------------|-------|
| बार-बार min/max extract | Heap | O(log n) insert + extract |
| Static data, एक बार sort | Sorted array | O(n log n) एक बार, O(1) access |
| Range queries + order stats | Balanced BST | O(log n) हर चीज़ के लिए |
| Stream से Top-K | Size K का Heap | O(n log k) total |

<Quiz question="Min-heap में कौन सा operation O(1) है?" options="Minimum ढूँढना|Minimum delete करना|Maximum ढूँढना|Median ढूँढना" answer="0" explanation="Minimum root (index 0) पर बैठता है, तो peek O(1) है। Delete करना O(log n) है क्योंकि sift down करना पड़ता है।" />

## Complexity Cheat Sheet

| Operation | Time |
|-----------|------|
| Insert | O(log n) |
| Extract min/max | O(log n) |
| Peek min/max | O(1) |
| Heapify | O(n) |
| n items से Top-K | O(n log k) |

<ThinkAboutIt>Heap array में store होने के बावजूद binary search क्यों नहीं कर सकते? सोचो heap property actually कौन सा sorted order guarantee करती है।</ThinkAboutIt>

---

## 📚 और पढ़ें

- [NeetCode - Heap / Priority Queue](https://neetcode.io/roadmap) - visual problem roadmap
- [Tech Interview Handbook - Heap](https://www.techinterviewhandbook.org/algorithms/heap/) - concise patterns और tips
- [Python heapq docs](https://docs.python.org/3/library/heapq.html) - official module reference
